打榜数据集文件路径为 /inspire/qb-ilm2/project/26summer-camp-01/public/benchmark.csv
使用 Agent 按题目顺序解答 benchmark.csv 中的每个问题，并记录下 Agent 的推理轨迹和答案，其中轨迹保存到 group_{}.json 文件中，答案保存在 group_{}.csv的 answer 列。
需要提交两个文件，一个是 Agent 的推理轨迹文件，另一个最终答案文件, 两个文件压缩成一个 zip 文件后提交。
轨迹文件命名格式为: group_{}.json
答案文件命名格式为: group_{}.csv
zip文件命名格式为: group_{}.zip 其中 {} 是你的组号。
打榜每半天更新一次成绩，成绩会在排行榜上展示。